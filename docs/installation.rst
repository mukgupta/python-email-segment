============
Installation
============

At the command line::

    $ easy_install python-email-segment

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv python-email-segment
    $ pip install python-email-segment